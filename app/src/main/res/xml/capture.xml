<?xml version="1.0" encoding="utf-8"?>
<!--
    The capture service. It gets two kinds of events: a text selection changed in an app, and a
    window came to the front. It can take a screenshot. It reads no other content.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeViewTextSelectionChanged|typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="true"
    android:canTakeScreenshot="true"
    android:description="@string/capture_description"
    android:notificationTimeout="50"
    android:summary="@string/capture_summary" />
